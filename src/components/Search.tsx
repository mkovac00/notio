import React from "react";
import "./Search.scss";

const Search = () => {
  return (
    <input
      className="search"
      type="text"
      placeholder="Search for a note..."
    ></input>
  );
};

export default Search;
